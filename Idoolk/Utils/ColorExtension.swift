import SwiftUI

struct AppColors {
    static let primary = Color(hex: "#93F996") // Purple
    static let secondary = Color(hex: "#4B0082") // Indigo
    static let background = Color(hex: "#161517") // Dark background
    static let surface = Color(hex: "#1E1E1E") // Slightly lighter dark
    static let tabBarBackground = Color(hex: "#1E1E1E")
    static let tabBarSelected = Color(hex: "#78F257") // Purple
    static let tabBarUnselected = Color(hex: "#777777") // Gray
    static let text = Color(hex: "#FFFFFF") // White
    static let secondaryText = Color(hex: "#BBBBBB") // Light gray
}

extension Color {
    init(hex: String) {
        let hex = hex.trimmingCharacters(in: CharacterSet.alphanumerics.inverted)
        var int: UInt64 = 0
        Scanner(string: hex).scanHexInt64(&int)
        let a, r, g, b: UInt64
        switch hex.count {
        case 3: // RGB (12-bit)
            (a, r, g, b) = (255, (int >> 8) * 17, (int >> 4 & 0xF) * 17, (int & 0xF) * 17)
        case 6: // RGB (24-bit)
            (a, r, g, b) = (255, int >> 16, int >> 8 & 0xFF, int & 0xFF)
        case 8: // ARGB (32-bit)
            (a, r, g, b) = (int >> 24, int >> 16 & 0xFF, int >> 8 & 0xFF, int & 0xFF)
        default:
            (a, r, g, b) = (255, 0, 0, 0)
        }
        self.init(
            .sRGB,
            red: Double(r) / 255,
            green: Double(g) / 255,
            blue: Double(b) / 255,
            opacity: Double(a) / 255
        )
    }
    
    func toHex() -> String {
        guard let components = cgColor?.components, components.count >= 3 else {
            return "#000000"
        }
        
        let r = Float(components[0])
        let g = Float(components[1])
        let b = Float(components[2])
        
        return String(format: "#%02lX%02lX%02lX", lroundf(r * 255), lroundf(g * 255), lroundf(b * 255))
    }
} 
